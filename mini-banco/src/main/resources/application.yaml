spring:
  application:
    name: mini-banco

  datasource:
    url: jdbc:mysql://localhost:3306/mini-banco
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect